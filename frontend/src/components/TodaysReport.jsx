export default function TodaysReport({ report = {} }) { const metrics = [['Calls dialed', report.calls_dialed], ['Calls connected', report.calls_connected], ['Applications sent', report.applications_sent], ['Interviews attended', report.interviews_attended]]; return <div className="panel rounded-lg p-4"><p className="label mb-4">Today’s report</p><div className="grid grid-cols-2 gap-3">{metrics.map(([label, value]) => <div key={label} className="border-l border-[#23415e] pl-3"><p className="text-xl font-black">{value ?? '—'}</p><p className="text-xs text-slate-500">{label}</p></div>)}</div>{Object.keys(report.method_breakdown ?? {}).length > 0 && <p className="mt-4 text-xs text-slate-400">{Object.entries(report.method_breakdown).map(([method, count]) => `${method}: ${count}`).join(' · ')}</p>}</div> }
